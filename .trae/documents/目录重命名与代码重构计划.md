# 重构计划：目录重命名与代码更新

根据您的要求，我将执行以下重构操作：

## 1. 目录结构变更

1. **Core 重命名**：将 `packages/render` 重命名为 `packages/hudx-render`。
2. **Shape 重命名**：将 `packages/hudx-render/src/shape` 重命名为 `packages/hudx-render/src/graphic`。

## 2. 包名与配置更新

1. **Package.json 更新**：
    * 修改 `packages/hudx-render/package.json` 的 `name` 为 `hudx-render`。
    * 更新 `packages/charts/package.json` 的依赖，将 `hudx-render` 替换为 `hudx-render`。
    * 更新根目录 `pnpm-workspace.yaml`（如果需要）。
2. **TSConfig 更新**：
    * 更新根目录 `tsconfig.json` 的 `paths` 映射，将 `hudx-render` 指向 `packages/hudx-render/src`。
    * 更新子包 `tsconfig.json` 中的引用路径。

## 3. 代码引用更新

1. **内部引用 (hudx-render)**：
    * 全局搜索并替换所有 `from './shape/...'` 为 `from './graphic/...'`。
    * 更新 `index.ts` 中的导出。
2. **外部引用 (charts)**：
    * 全局搜索并替换所有 `from 'hudx-render'` 为 `from 'hudx-render'`。

## 4. 文档更新

* 更新项目中的 README 文档，反映包名变更。

## 5. 验证

* 执行 `pnpm install` 更新依赖链接。
* 执行构建命令确保重构无误。
