# 文档更新与测试补全计划

我将执行以下操作来更新文档并确保所有包的测试覆盖率和正确性。

## 1. 文档更新 (`docs/en/API_REFERENCE.md`)
*   **Legend 配置**：更新 `ChartOption.legend` 部分，说明 `data` 属性支持对象数组，不仅仅是字符串数组。将明确指出可以在数据项中配置 `icon` 和 `textStyle`（包括 `color`, `fontSize`, `fontWeight`, `fontFamily`）。
*   **X轴配置**：更新 `ChartOption.xAxis` 部分，说明如果 `type` 未定义但提供了 `data`，系统将自动推断为 `'category'` 类型。

## 2. 测试补全 (`packages/render`)
*   **Legend 测试**：在 `packages/render/src/component/__tests__/Legend.test.ts` 中添加一个新的测试用例，专门验证 `Legend` 组件是否正确应用了每个 Item 的自定义 `textStyle`。这将防止未来回归。

## 3. 验证
*   运行 `pnpm test` 在 `packages/render` 和 `packages/charts` 中，确保所有现有测试和新添加的测试都通过。
